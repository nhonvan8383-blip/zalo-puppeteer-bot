{
  "name": "zalo-puppeteer-bot",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && npx puppeteer browsers install chrome"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "express": "^4.18.2",
    "form-data": "^4.0.0",
    "puppeteer": "^22.0.0"
  }
}
